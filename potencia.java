public class Potencia extends OperacionMatematica {
    @Override
    public double realizarOperacion(double a, double b) {
        return Math.pow(a, b); // Utiliza la función de Java para potencia
    }
}
